import React from 'react';
import {
  View,
  Text,
  SafeAreaView,
  TouchableOpacity,
  StyleSheet,
} from 'react-native';

const NavBar = () => {
  return (
    <View>
      <Text>Nav bar</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  
})

export default NavBar;
